# ─────────────────────────────────────────────────────────────────────────────
# Hyperliquid Bot - Minimal required environment
# I parametri strategia (trend/scalping) ora si gestiscono dal frontend
# e vengono salvati in state/runtime_config.json.
# ─────────────────────────────────────────────────────────────────────────────

# Core runtime
EXECUTION_MODE=live
ENABLE_MAINNET_TRADING=false
HYPERLIQUID_BASE_URL=https://api.hyperliquid.xyz
HYPERLIQUID_INFO_TIMEOUT=15
HYPERLIQUID_EXCHANGE_TIMEOUT=30

# Credentials
HYPERLIQUID_WALLET_ADDRESS=0xYOUR_WALLET
HYPERLIQUID_PRIVATE_KEY=0xYOUR_PRIVATE_KEY
OPENROUTER_API_KEY=YOUR_OPENROUTER_KEY
DASHBOARD_API_KEY=YOUR_DASHBOARD_KEY

# LLM (fixed model)
LLM_MODEL=deepseek/deepseek-v3.2
LLM_MAX_TOKENS=8192
LLM_TEMPERATURE=0.2

# Trading universe defaults (override by frontend runtime config)
TRADING_PAIRS=BTC,ETH,SOL

# Runtime defaults
DEFAULT_STRATEGY_MODE=trend

# Optional logging/state paths
LOG_LEVEL=INFO
LOG_FILE=logs/hyperliquid_bot.log
STATE_PATH=state/bot_state.json
METRICS_PATH=state/bot_metrics.json